export type Tab = "schedule" | "codes" | "dailies";

/** The views, in the order the tab bar shows them. The window opens on the first. */
export const TABS: readonly Tab[] = ["schedule", "codes", "dailies"];
